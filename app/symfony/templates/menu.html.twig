{% extends 'base.html.twig' %}

{% block menu %}
<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a href="{{ path('app_dashboard') }}" class="navbar-brand">Tickets app</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <div class="navbar-nav">
            <a href="{{ path('app_dashboard') }}" class="nav-item nav-link active">Tickets</a>
            <a href="{{ path('app_create_ticket') }}" class="nav-item nav-link">Create ticket</a>
            <a href="{{ path('app_register') }}" class="nav-item nav-link">Register</a>
            <a href="{{ path('app_logout') }}" class="nav-item nav-link">Log out</a>
        </div>

    </div>
</nav>
{% endblock %}